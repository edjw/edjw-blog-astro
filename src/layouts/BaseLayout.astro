---
import "../styles/global.css";

import siteData from "../data/siteconfig.js";
import BaseHead from "../components/BaseHead.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";

const { pageTitle, description: pageDescription } = Astro.props;

const {
  title: siteTitle,
  url,
  author: { twitterUsername },
  repository,
  RSSFeed,
  sitemap,
  contentLicence,
} = siteData;
---

<html lang="en">
  <head>
    <BaseHead title={pageTitle} description={pageDescription} />
  </head>
  <body class="bg-white dark:bg-trueGray-800">
    <div
      class="container grid grid-rows-[auto_1fr_auto] h-screen max-w-4xl mx-auto pt-4 px-4"
    >
      <Header />

      <main class="mt-8 prose dark:prose-dark">
        <slot />
      </main>

      <Footer />
    </div>
  </body>
</html>
